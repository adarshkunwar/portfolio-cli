import React from 'react'

type props = {
  children: React.ReactNode;
}
// TODO: Things needed here are: 1. a search bar, 2. sample commands, 3. output field
const Cli: React.FC<props> = ({ children }) => {
  return (
    <div>Cli: </div>
  )
}

export default Cli
